import {test} from 'node:test';import assert from 'node:assert/strict';
import {artworkPhase,inProduction,saleSummary} from '../lib/artwork-lifecycle.ts';
test('completed imported paintings are inventory, never production',()=>{assert.equal(artworkPhase('Available'),'Available Inventory');assert.equal(inProduction({type:'ARTWORK',statusEnum:'PLANNED',artworkAvailability:'Available'}),false);assert.equal(inProduction({type:'ARTWORK',statusEnum:'ACTIVE',artworkAvailability:'Sold'}),false);assert.equal(inProduction({type:'ARTWORK',statusEnum:'PLANNED',artworkAvailability:'Not ready'}),true);});
test('profit uses actual sale terms, refunds and known costs; missing is not zero',()=>{const sale={salePriceCents:15000,shippingIncomeCents:2000,salesTaxCents:1000,sellingFeesCents:null,shippingExpenseCents:null,transactions:[]};const p={materialsCostCents:null,framingCostCents:null};assert.deepEqual(saleSummary(sale,p),{receivedCents:0,balanceCents:18000,paymentStatus:'Unpaid',knownCostsCents:0,costsComplete:false,profitCents:17000});const known=saleSummary({...sale,sellingFeesCents:500,shippingExpenseCents:1000,transactions:[{type:'INCOME',amountCents:18000,salesTaxCents:1000,shippingCents:2000,receivedAt:'2026-09-25'},{type:'REFUND',amountCents:3000,salesTaxCents:200,shippingCents:0,receivedAt:'2026-09-26'}]},{materialsCostCents:2000,framingCostCents:0});assert.equal(known.profitCents,10700);assert.equal(known.receivedCents,15000);assert.equal(known.costsComplete,true);});
